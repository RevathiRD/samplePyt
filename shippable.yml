# Language setting
language: python

# Version number
python:
  - 2.6
 # - 2.7
  #- 3.2
  #- 3.3
  #- 3.4
  #- 3.5
  #- pypy
  #- pypy3

build:
  ci:
    - python --version
    
integrations:
  notifications:
    - integrationName: ship-issues
      type: webhook
      payload:
        - title=Failure - $REPO_FULL_NAME - $BUILD_NUMBER
        - body=Shippable Run $BUILD_NUMBER ($BUILD_URL) failed for $COMPARE_URL
